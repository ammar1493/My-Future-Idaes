# Base URL of the backend API. Leave blank for local dev (Vite proxies /api to
# localhost:8000). In production (Vercel) set this to your backend deployment,
# e.g. https://livetrain-api.vercel.app
VITE_API_BASE=
